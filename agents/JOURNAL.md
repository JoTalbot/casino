# JOURNAL.md — Журнал смен

> **APPEND-ONLY.** Новые записи добавляются **вниз**.
> Прошлые записи никогда не редактируются и не удаляются.
> При merge-конфликте сохраняются **обе** записи.

---

## 2026-08-17 — claude-2026-08-17-A

**SHIFT START** ~12:00 UTC
Задачи: T-001 … T-005
Локи: не ставились (репозиторий пустой, параллельных агентов не было)

### Контекст на входе
Репозиторий `JoTalbot/casino` существовал, но был **полностью пустым** — ни одного коммита,
ни инструкций, ни истории. Задача от владельца: настроить репозиторий, создать инструкции
для всех будущих агентов (работа ведётся с разных машин, разными ИИ, в том числе параллельно,
нужно сохранять статус и контекст), и провести глубокое исследование по теме
«как сделать своё личное интернет-казино со слотами».

### Сделано
- **T-001** Репозиторий инициализирован, ветка `main`, `.gitignore`, `README.md`.
- **T-002** Написан `AGENTS.md` v1.0 — полный протокол работы агентов:
  - правила git (pull --rebase, атомарные коммиты, push сразу, Conventional Commits);
  - идентификация агента (`<модель>-<дата>-<буква>`);
  - **файловые локи** в `agents/locks/` для параллельной работы, разрешение гонок «кто первый запушил»;
  - чеклисты начала и конца смены;
  - форматы JOURNAL / TASKS / DECISIONS;
  - правила исследования (всё со ссылками, разделять факт и оценку);
  - правила кода (CSPRNG, серверный RNG, деньги в целых, полный лог раундов);
  - юридическая рамка §11 — запрет на реальные деньги без лицензии;
  - таблица анти-паттернов.
- **T-003** Создана система сохранения контекста: `STATE.md`, `HANDOFF.md`, `TASKS.md`,
  `JOURNAL.md`, `DECISIONS.md`, `QUESTIONS.md`, `GLOSSARY.md`, `locks/`.
- **T-004** Проведено глубокое исследование, оформлено 5 документов в `research/`:
  - `01-ARCHITECTURE.md` — стек, серверная архитектура, provably fair, RGS, безопасность;
  - `02-SLOT-MATH.md` — reel strips, PAR sheet, RTP, hit frequency, волатильность,
    виртуальные барабаны (патент Telnaes), формулы free spins с retrigger, Monte Carlo;
  - `03-LEGAL.md` — юрисдикции (Кюрасао/Анжуан/Мальта/Kahnawake), реальные цены 2026,
    сертификация RNG (GLI-11, iTech, eCOGRA, BMM), KYC/AML, ответственная игра, риски;
  - `04-ECONOMICS.md` — build vs buy, white label / turnkey / кастом, бюджеты,
    юнит-экономика (GGR/NGR, rev share, PSP, аффилиаты), сроки;
  - `05-COMMUNITIES.md` — форумы, Discord/Telegram, subreddit'ы, конференции,
    open-source репозитории, предупреждение про nulled-скрипты и бэкдоры.
- **T-005** Собран реестр источников `research/SOURCES.md`.

### Решения
- **ADR-001** Формат мультиагентной работы: файловые локи + append-only журнал.
- **ADR-002** Provably fair на HMAC-SHA256 (commit-reveal) как базовая схема RNG.
- **ADR-003** Русский язык для всей документации, английский для идентификаторов в коде.

### Проблемы / не доделано
- Кода нет — смена была полностью документационной и исследовательской.
- **Главная блокировка:** не определён тип продукта (соц-казино / B2B-движок /
  лицензированный оператор). Это Q-001, задача T-006. От ответа зависят стек,
  юрисдикция, бюджет и почти все дальнейшие ADR.

### Для следующего агента
Читай `agents/HANDOFF.md` — там подробно.
Коротко: если Q-001 не отвечен — бери **T-007** (PAR sheet + симулятор RTP),
теория уже разобрана в `research/02-SLOT-MATH.md`, заново искать не нужно.
Потом **T-008** (provably fair RNG, схема в `research/01-ARCHITECTURE.md` §4).

**SHIFT END**

---
<!-- Следующие агенты: добавляйте свои записи НИЖЕ этой строки -->

## claude-2026-08-17-A — смена #1, дополнение (публикация)

**Время:** 2026-08-17

- Первый коммит `cda4f60` запушен в `origin/main`.
- Состав: 21 файл — `AGENTS.md`, `README.md`, `.gitignore`,
  `agents/` (7 документов + `locks/`), `research/` (5 документов + `SOURCES.md`),
  пустые каталоги `docs/`, `src/`, `scripts/` с `.gitkeep`.
- Добавлен `agents/locks/README.md` — краткая инструкция по механике локов
  прямо в каталоге, чтобы агент нашёл её, не перечитывая весь `AGENTS.md`.
- Push проверен: `git ls-tree -r origin/main` совпадает с локальным деревом.
- Активных локов нет — область свободна для следующего агента.

**Замечание для следующего агента:** `gh` CLI в окружении недоступен,
`api.github.com` в момент смены отвечал ошибкой. Git по HTTPS с токеном работает.
Если нужен GitHub API — проверь доступность заново, возможно, сбой был временный.

## 2026-08-17 — claude-2026-08-17-B

**SHIFT START** 12:00 UTC
Задачи: T-006 … T-018 (полный бэклог по команде владельца «делай всё»)
Локи: `math`, `engine`, `backend`, `frontend`, `infra`, `docs`, `research` — все области,
работаю один, параллельных агентов нет.

### Замечание по окружению
`.git/config` не сохранился между сессиями (исключён из снапшота как чувствительный путь).
Пришлось заново добавить remote. Если следующий агент видит
`fatal: 'origin' does not appear to be a git repository` — это оно, надо восстановить remote.
