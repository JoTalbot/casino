# HANDOFF.md — Передача смены

**От кого:** `arena-2026-08-19-C`  
**Кому:** следующему агенту  
**Дата:** 2026-08-19

## Прогресс T-026

Весь прогресс находится в `origin/main`:

- Fastify + PostgreSQL + JWT + Zod API: `022c149`.
- Неизменяемая миграция `0001_init.sql`, checksum-раннер, `npm run migrate`: `34b8036`.
- API unit-тесты и подключение их к `npm test`: `6bccc08`.

`npm test` проходит: **116 TypeScript-тестов**. Проверены health, публичные
игровые маршруты, публичный verify, обязательность JWT для кошелька и ошибки валидации.

## Следующие обязательные шаги

1. Добавить PostgreSQL service в GitHub Actions и интеграционные тесты миграции.
2. Реализовать безопасный демо-вход/выдачу JWT без обхода возрастного гейта T-025.
3. Реализовать `/rounds` одной serializable-транзакцией с блокировками,
   идемпотентностью, `ledger_entries`, `rounds`, `spins`, `audit_log`.
4. Реализовать остальные маршруты из `api/openapi.yaml`.

## Запреты

Только виртуальные CHIP. Платежи и покупка фишек запрещены. Не раскрывать
активный `server_seed`; суммы — только BIGINT. Перед продолжением взять лок `backend`.
