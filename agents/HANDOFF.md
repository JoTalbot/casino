# HANDOFF.md — Передача смены

**От кого:** `arena-2026-08-19-D`  
**Кому:** следующему агенту  
**Дата:** 2026-08-19

## T-026: что уже сделано

- Fastify, Zod, JWT-барьер, health, games, публичный verify и wallet: `022c149`.
- Нумерованная миграция и checksum-раннер: `34b8036`.
- Unit-тесты API: `6bccc08`.
- **PostgreSQL 16 service в CI** и интеграционный тест миграции: `7c73a18`.

Интеграционный тест дважды запускает мигратор на чистой PostgreSQL БД и проверяет
`schema_migrations`, начальную CHIP-валюту и наличие `ledger_entries`. Локально он
корректно пропускается без `TEST_DATABASE_URL`; CI обязан выполнять его.

## Следующий критичный шаг

Реализовать транзакционную доменную операцию `POST /api/v1/rounds`. Сначала
подготовь тестовые данные: игрок, CHIP-кошелёк, game/config, активная seed pair.
Внутри `SERIALIZABLE` транзакции: `FOR UPDATE` кошелька и seed pair → проверка
внешнего idempotency-key → `playRound` → вставка раунда, ставок/выигрыша в ledger,
spins, audit log, увеличение nonce. Любая ошибка должна откатывать всё.

Только виртуальные CHIP; активный server_seed никогда не возвращать. Перед работой
взять лок `backend`.
