# STATE.md — Текущее состояние проекта

> Живой документ. Обновляется **каждым** агентом.

**Последнее обновление:** 2026-08-20
**Обновил агент:** arena-2026-08-20-R (до него: A…Q, 2026-08-19/20)

---

## 1. Стадия проекта

```
[X] Стадия 0 — Инициализация репозитория и протокола агентов
[X] Стадия 1 — Глубокое исследование
[X] Стадия 2 — Прототип математики слота
[X] Стадия 3 — Прототип игрового движка
[X] Стадия 4 — Фронтенд-клиент слота
[X] Стадия 5 — Платформа: аккаунты, кошелёк, история, админка
[X] Стадия 6 — Юридическая подготовка
[X] Стадия 7 — Деплой и мониторинг
[X] Стадия 8 — Доп. фичи: daily bonus, leaderboard, master RTP, GDPR, Playwright
[ ] Стадия 9 — Сертификация и платежи (заблокирована лицензией)
```

**Текущая стадия: 8.** Задачи закрыты до T-181.

⚠️ **Важно про историю.** Аудит T-174 показал: смены H…Q (задачи T-104…T-173)
закрывались по чеклисту, но не меняли код — только `agents/**`. Реальная
функциональность заканчивается на T-103. Записи в TASKS.md вида «Final polish»
за этот период доверия не заслуживают: сверяйтесь с `git log --stat`.

---

## 2. Что готово (R)

- **Daily bonus (T-049):** POST /bonus/daily 1000 CHIP раз в сутки, idempotent по дате
- **Leaderboard (T-050):** GET /leaderboard?by=win|bet&period=day|week|all
- **Master RTP (T-051):** выбор game_config ближайший к player.master_rtp, поддержка нескольких RTP наборов
- **GDPR export (T-052):** GET /me/export — профиль, кошельки, раунды, ledger, лимиты, exclusion, seeds
- **Playwright e2e UI (T-053):** tests/e2e/client.spec.ts — age gate, спин, история, RG

Плюс всё предыдущее: 2 игры, деплой, админка с графиками и поиском, история, верификатор, rate limiting, reality check, e2e API, hardening, README.

### Смена R (2026-08-20) — багфиксы и покрытие

- **T-177:** `/api/v1/referrals/progress` отвечал 500, `/referrals/leaderboard` — 404:
  регистрация второго маршрута лежала внутри обработчика первого. Починено.
- **T-180:** рефералка позволяла начислить себе 5000 CHIP с произвольным UUID.
  Теперь проверяются существование обоих игроков и «новизна» приглашённого,
  ошибки разведены по кодам (SELF_REFERRAL, REFEREE_NOT_NEW, …).
- **T-179:** ачивка и награда за неё — одна транзакция; гонка больше не даёт
  двойного начисления.
- **T-178:** push-подписки перенесены из памяти процесса в PostgreSQL
  (миграция `0004_push.sql`), добавлен `DELETE /api/v1/push/subscribe`.
- **T-175:** `src/server/fakeDb.ts` — стаб БД; соц-слой впервые под тестами,
  которые реально идут в CI (ADR-020).
- **T-176:** чат — валидация длины после trim, чистка счётчиков rate limit.
- **T-181:** `docs/API.md` §3.8 — соц-эндпоинты и коды ошибок.
- **ADR-021:** `check_protocol.py` предупреждает о серии коммитов без кода.

**Тесты:** 136 TS (3 PG skip) + 35 round + 37 confidence + 21 клиент.

---

## 3. Юридический статус

| Параметр | Значение |
|---|---|
| Лицензия | ОТСУТСТВУЕТ |
| ToS/Privacy | готово |
| Age gate 18+ | готово |
| Режим денег | ЗАПРЕЩЁН — только CHIP |
| GDPR export | готово (T-052) |

Q-006 🔴 открыт.

---

## 4. Что НЕ готово

- Выбор юрисдикции Q-002, бюджет Q-003, фиат/крипта Q-004, агрегатор Q-005 — открыты, не блокируют
- Сертификация RNG и платежи — заблокированы лицензией
- PAT утечка — требует ротации

---

## 4.1 Развёрнутый стенд (2026-08-20)

| Параметр | Значение |
|---|---|
| Хост | 167.233.95.7, каталог `/root/casino` |
| Запуск | `docker compose -p casino -f docker-compose.yml -f deploy/docker-compose.shared-host.yml up -d` |
| API | `127.0.0.1:21110` (наружу не открыт) |
| Клиент | `127.0.0.1:21899` (наружу не открыт) |
| PostgreSQL | только внутри compose-сети, порт не публикуется |
| Секреты | `/root/casino/.env`, chmod 600, в git не попадают |
| Сервер общий | nginx 80/443 и 9 чужих контейнеров — не тронуты, UFW не менялся |

Деплой вскрыл четыре бага, невидимых для CI: T-184 (демо-вход), T-185
(RULE против ON CONFLICT), T-186 (BigInt в JSON), T-187 (ADMIN_TOKEN).
Все починены, на живой базе 139/139 тестов без пропусков.

---

## 4.2 Клиент (2026-08-20)

- Спин чинён: демо-вход падал из-за пустого JSON-тела (T-189).
- Графика переделана: векторные символы и корпус автомата, эффекты
  выигрыша (T-190). Внешних ассетов нет.
- Сцена запекается в текстуры; раунд ускорился с 12 с до 4.8 с в
  софтверном рендере (T-191).
- Символы — 3D-рендеры (10 из 11; символ 10 ждёт своей картинки, T-194).
  Объёмные эффекты: цилиндрическая подсветка барабана, блик по раме,
  фонтан монет, параллакс, тряска камеры (T-193).

---

## 4.3 Telegram Mini App (2026-08-20)

| Параметр | Значение |
|---|---|
| Бот | `@CasinoSlottor_bot` |
| Адрес мини-аппа | `https://api.autosklo.org.ua/parts/casino/` |
| Почему такой путь | Домен закрыт Cloudflare Access, наружу пропускается только `/parts/*` (ADR-023) |
| Вход | По подписи `initData`, привязка к `players.telegram_id` |
| Вебхук | `/parts/casino/api/v1/telegram/webhook`, защищён секретным заголовком |
| Бонус-игра | «Сундуки короны»: форма серии фриспинов, EV-нейтральна (ADR-022) |

⚠️ Токен бота присылали в чат — подлежит замене через @BotFather.

---

## 4.4 Что добавлено в смену S (2026-08-20)

- **Автоспин** с условиями остановки: бонус, крупный выигрыш, порог баланса,
  цель по выигрышу; напоминание о времени прерывает серию.
- **Объявление бонуса**: сундук с фонтаном монет перед серией фриспинов.
- **Приглашения через бота**: диплинк `?start=ref_<id>`.
- **T-201:** починена гонка интеграционных тестов, которая роняла CI через раз.
- **T-182 закрыта задним числом:** PostgreSQL в CI работает с 2026-08-19,
  156 тестов, нуль пропусков. Прошлая запись об обратном была ошибкой.

---

## 5. Следующий шаг

Следующий свободный ID — **T-183**. В бэклоге открыт **T-182**: поднять
PostgreSQL сервисом в CI, чтобы `*.integration.test.ts` перестали
пропускаться (сейчас 3 теста skip, и именно в этой слепой зоне жили баги
T-177 и T-180). Дальше по ценности: мобильный UI, графики в клиенте,
блок по IP. Реальные выплаты — только после лицензии.

Владельцу: отозвать PAT, ответить Q-006.

