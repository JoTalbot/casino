# Python-зависимости математики и аналитики.
#
# Установка:
#     pip install -r requirements.txt
#
# Верхняя граница стоит намеренно: numpy меняет поведение между
# мажорными версиями (в 2.0 менялись правила приведения типов), а от
# численных результатов здесь зависят RTP, фикстуры и configHash.
# Обновление мажора — отдельная задача с пересчётом кросс-проверки,
# а не побочный эффект установки на новой машине.
numpy>=1.26,<3.0
